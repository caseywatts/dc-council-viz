{
  "name": "dc-council-viz",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview",
    "publish": "git status --porcelain && npm run build && git add dist && git commit -m 'commit dist for github-pages' && git push && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.30",
    "autoprefixer": "^10.4.7",
    "postcss": "^8.4.14",
    "prettier": "2.6.2",
    "svelte": "^3.44.0",
    "tailwindcss": "^3.0.24",
    "vite": "^2.9.9"
  },
  "dependencies": {
    "canvas2image-2": "^2.3.0",
    "dc-council-lims": "^0.1.2",
    "html2canvas": "^1.4.1"
  }
}
